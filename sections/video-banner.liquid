<style>
      .video-section {
    position: relative;
    padding-top: 56%
  }

  .video-section video {
      position: absolute;
      top:0;
      left: 0;
      width: 100%;
      height: 100%;
      object-fit:cover;
  }
</style>
<section class="video-section">
  {% if section.settings.banner_video != blank %}
    {{ section.settings.banner_video | video_tag: autoplay: true, loop: true, muted: true, controls: false }}
  {% else %}
    {{ section.settings.url | video_tag: autoplay: true, loop: true, muted: true, controls: false }}
  {% endif %}
</section>

{% schema %}
{
  "name": "Video Section",
  "settings": [
    {
      "type": "video",
      "id": "banner_video",
      "label": "Banner Video"
    },
    {
      "type": "url",
      "id": "video_url",
      "label": "Video URL",
    }
  ],
  "presets": [
    {
      "name": "Video Section"
    }
  ]
}
{% endschema %}
